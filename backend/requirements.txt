# Dependencias del backend — VERSIONES FIJAS.
#
# ⚠️ Antes decían todas `>=`, sin archivo de bloqueo. Cada build de Railway
# resolvía versiones nuevas por su cuenta: una publicación rota —o comprometida—
# entraba sola al despliegue, y dos builds del mismo commit podían no ser el
# mismo software. El frontend ya estaba cubierto por `package-lock.json`; esto
# era el único lado sin piso.
#
# Estas son las versiones con las que corre la suite en verde (2026-08-28).
# Para subir alguna: cambiar el número acá, correr `python -m pytest -q`, y que
# el cambio de versión se vea en el commit — que es justamente lo que `>=`
# impedía.
fastapi==0.138.2
python-multipart==0.0.32
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
asyncpg==0.31.0
alembic==1.19.1
python-dotenv==1.2.2
pandas==3.0.3
openpyxl==3.1.5
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
tzdata==2026.2
anthropic==1.0.0
# El generador del Word del cierre. Fijada como el resto: con `>=`
# cada build resuelve una versión distinta por su cuenta.
python-docx==1.2.0
